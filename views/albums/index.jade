extends ../layout

block content
  h1 Albums

  a(href="/albums/new") New Album

   table
    thead
      tr
        th ID
        th Name
    tbody
      for album in allAlbums
        tr
          td= album._id
          td
            a(href='/albums/' + album._id)= album.name